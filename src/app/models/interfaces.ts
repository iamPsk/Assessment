export interface Slide {
  title: string,
  img: string
}

export interface Card {
  title: string,
  description: string,
  icon?: string
}

export interface Experience {
  title: string,
  description: string,
  company: string,
  href?: string,
}